# 2026-09-12

## Personal library cleanup and Settings

- Removed the Library path and Open library group from Settings. Folder selection
  remains in the main toolbar. This hides the control; it does not move the files.
- Normal startup opens the last library or an empty view. Bundled fixtures now
  require an explicit demo, stress, or missing-cover flag.
- Confirmed sync reuses completed cover, description, and review stages. Existing
  games still receive playtime updates. New games download metadata and covers.
  Failed stages retry; manual Refresh cover deliberately fetches again.
- Added a request-count test: first sync fetches once per stage; repeat sync adds
  zero downloads; adding one game adds exactly one request per stage.
- Matched demo provenance using original history, fixture description, title, App
  ID, and personal cover path. Four records had only their original demo revision:
  Hogwarts Legacy, Elden Ring, Portal 2, and Outer Wilds. Archived them through the
  revision store. No history or media was deleted.
- Eight demo records also belonged to the synced library. Kept those games and
  cleared seeded status, rating, favorite, tags, and cover only where every
  historical revision kept the original value. Preserved later edits, notes, and
  collection membership. Used revision checks for each write. The temporary
  cleanup helper was removed after use.
- Result: 201 active synced games; four demo-only records archived. All active
  games now have Backlog status because the other statuses came from demo data.
- Checks: 67 offline tests passed; build, format, Clippy with warnings denied,
  and diff checks passed. Native macOS shows 201 games. Opened Settings with
  Command-comma and confirmed Theme goes directly to Steam, without Library.
- No real-key sync was triggered for verification. Linux remains unverified.
- The personal library still lives in the original temporary QA folder. Next:
  choose a permanent library location and move it with verification.


## Inline details and desktop polish

- Removed the library labels below Settings, toolbar theme menu, and toolbar
  Refresh button. Refresh shortcuts and file watching still work. The footer
  shows the last completed Steam sync, including partial runs; cancellation does
  not advance the time. If older runs have no stored time, say so explicitly.
- Details start hidden. Status and Collections can collapse. The Collections
  header has a small plus button that opens the existing collection controls.
- Inspector fields are always editable. Status, stars, favorite, and collection
  membership save on click. Text saves after a 450 ms pause. Reuse existing
  revision checks; retain failed drafts and expose retry/discard recovery.
  Text stays editable during a write; later changes queue another save. Provider
  updates can rebase only if the personal base did not change. Close and folder
  switches still protect unsaved work. Successful data stays in revision history.
- Settings reuses Eagle's left category navigation and grouped content. General:
  masked Steam profile/key and sync. Look and feel: all themes and Reduce motion.
  AI: placeholders for local models and provider keys; no AI calls or secrets yet.
- Test key validates without saving. Save key is enabled only for the tested
  input pair. Replacing either field requires another test. Successful save clears
  the key field and pending test. Repeated storage of the same key is a no-op.
  Failed replacement retains the prior credential. Session-only use stays explicit.
- Motion follows the [Eagle review](../motion-review.md): reversible 200 ms
  ease-out sidebars and section clips. Density reflows once and resizes visible
  artwork in clipped cells. Reduce motion removes movement. No search or keyboard
  selection animation, subtree fades, new dependency, or continuous idle loop.

### Checks

- 69 offline tests passed. Build, formatting, Clippy with warnings denied, and
  diff checks passed. Tests include changed credential inputs, repeated saves,
  failed replacements, revision conflicts, and existing sync/cache behavior.
- macOS Keychain dummy value survived a new entry and a separate process; both
  checks removed the test key. No real Steam key was read by a diagnostic script.
- Native checks used a separate QA library: direct star save, tag autosave, note
  autosave, data on disk and rating after restart, hidden inspector on startup,
  section reversal, plus-button collection controls, all Settings categories,
  masked dummy inputs, rejected key test with Save disabled, and Reduce motion.
- Native inspection exposed two small layout issues; aligned the Status header
  and cleared idle Steam feedback when switching Settings categories.
- Restored the user's motion preference and reopened the 201-game personal
  library. No personal ratings, notes, memberships, or credential were changed.
- Limits: Linux and frame-time profiling remain unverified. Custom GPUI controls
  lack native accessibility-tree exposure. A forced process exit can lose text
  that has not reached its autosave. The real-key success flow awaits user testing;
  secure persistence was verified with dummy credentials instead.

| Before | After | Why |
| --- | --- | --- |
| Edit, Save, Done workflow | Inline fields with save feedback | Reduce steps for routine changes |
| Theme menu and refresh control in toolbar | Themes in Settings; sync time in footer | Keep browsing controls focused |
| Large collection management button | Plus beside a collapsible heading | Follow the supplied reference |
| One long Settings page | General, Look and feel, AI | Keep related options together |

- Final personal-library smoke: 201 games, inspector hidden, no old toolbar or
  sidebar labels, updated footer, and both compact/normal cover layouts rendered.
  Returned the grid to normal size. No user record was edited during this check.
