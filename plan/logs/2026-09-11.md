# 2026-09-11

## Documentation foundation

### Decisions

- Keep the desktop app in this repository under `desktop/`.
- Use `plan/` for project documents and short work logs.
- Keep the product small and cozy. Use Eagle as the GUI reference.
- Apply Apple Design, Product Design, and Emil principles within Rust/GPUI.
- Use short technical English and focused, commented code.
- See [the app plan](../app-plan.md) for product and architecture decisions.

### Changes

- Created branch `codex/desktop-foundation` from `main`.
- Added root agent guidance and the plan index, app plan, guardrails, and design reference.
- Left application code and both source projects unchanged.

### Evidence

- The working tree was clean before this documentation change.
- Product Design preflight found no saved plugin context. The supplied Eagle reference is the design source.
- Reviewed Eagle's motion notes and primitives for GPUI-specific limits.
- Checked all six documents: 12 local links resolve and no whitespace errors were found.
- Confirmed that the only new files are `AGENTS.md` and the five plan documents.
- Earlier source analysis found a passing GameSync TypeScript check. No new application build or test is needed for this documentation change.
- Desktop implementation and native visual checks have not started.

### Next task

Build milestone 1: the native fixture grid and inspector. Capture the current
Eagle reference before matching its appearance. Record macOS and Linux
checks separately.

## Native demo

### Changes

- Added the Rust/GPUI package under `desktop/` on the same feature branch.
- Reused Eagle's cache, theme files, theme handling, grid virtualization, and desktop layout patterns. See [source notes](../../desktop/SOURCE-NOTES.md).
- Added 12 demo games with offline covers, status/favorite filters, search, arrow-key selection, and a read-only inspector.
- Added pane toggles, cover density, appearance selection, search/quit shortcuts, and a development launcher.
- Added empty, missing-cover, and 10,000-record fixture modes.
- Kept storage, Steam, AI, and other views out of this milestone.

### Validation

- macOS arm64, Rust 1.94.0: build, 12 tests, Clippy with warnings denied, and formatting checks passed.
- Opened the existing Eagle binary for a native reference. Its bundled build differs from the current source; no claim of exact screenshot fidelity is made.
- Inspected the GameSync native window: cover grid, selected-game details, combined search/status filters, no results, and status counts.
- Verified arrow keys, Command-F search, Command-Q exit, inspector toggle, light appearance, Catppuccin Mocha, and native window zoom/reflow.
- Scrolled the 10,000-record fixture and filtered it to 834 Stardew Valley records. This was a usability smoke check, not a frame-time benchmark. Records share 12 covers; unique-image cache pressure remains unmeasured.
- Verified missing covers in both grid and inspector, plus a truly empty library.
- The native accessibility tree exposes the window but not the custom controls in this pinned stack. Visual and pointer checks do not establish screen-reader support.
- Linux runtime checks remain pending. Only `aarch64-apple-darwin` is installed here.

### Design review

| Before | After | Why |
| --- | --- | --- |
| Eagle's square asset tiles and file metadata | Portrait covers, game captions, and game details | Fit the library task while retaining the proven layout |
| Fixed-width search beside both panes | Search can shrink within the toolbar | Keep controls usable when the center pane narrows |
| Window close leaves a demo process without a window | Quit when the last window closes | Make development relaunch predictable |
| Arrow selection recenters visible rows | Reveal only rows outside the viewport | Keep navigation steady |

### Next task

Run the same native checks on Linux. Then implement milestone 2 with file
persistence and revision recovery before enabling personal edits.

## Commit and storage file layer

### Changes

- Committed the documented native demo as `71fcea7` (`feat: add native game library demo`).
- Started platform-independent storage work while Linux UI checks remain pending. Added no screens.
- Added a small Rust library module for JSON publication. It retains an immutable revision before replacing a current record.
- Stage files beside their destination, flush their contents, and sync directory entries after publication. Temporary files have a `.tmp` suffix.
- Reject revision-path reuse with different content. Permit an exact retry after a failed current-file save.
- Use the already locked `tempfile` package. Eagle has no matching revision writer to reuse.
- Keep the file layer separate from the demo. Schema validation, safe path selection, revision parents, and conflict checks are caller requirements.

### Validation

- macOS arm64: all 20 tests passed, including eight new storage tests.
- Formatting and Clippy with warnings denied passed. The dependency lock adds no new package.
- Tested retained revisions, exact retry after current-file failure, missing history, serialization failure, incomplete temporary files, concurrent revision publication, and reads during replacement.
- Failure cases use controlled filesystem conditions. They do not simulate physical power loss, Dropbox synchronization, or disk removal.
- Linux checks remain pending. Windows writes return an unsupported-platform error.

### Next task

Add versioned library/game records and revision reconciliation. Detect stale edits
and preserve conflict branches before connecting saves to the inspector. Run the
native demo on Linux before adding new screens. Add the local index with the
library reader; the file layer alone does not need SQLite.

## Versioned game records and conflicts

### Changes

- Added typed version 1 game records with UUID identity and revision parents.
- Kept Steam data separate from personal values. Preserved unknown supported fields and explicit blank description overrides.
- Added guarded personal edits, reversible tombstones, and explicit branch resolution over the existing file layer.
- Scan immutable history and UUID-prefixed current-file conflict copies. Preserve branches before resolution and reject changed revision sets.
- Block writes for incomplete history, cycles, changed revision content, newer formats, invalid values, unsafe cover paths, and oversized records.
- Added OS locks for local writers. Locks do not coordinate Dropbox or other computers.
- Reused the existing locked `uuid` package. No new package was added to the lock.
- Kept the demo unchanged. The service requires existing library data folders; library definitions and UI integration remain pending.

### Validation

- macOS arm64: all 31 tests passed, including 11 new record-store tests.
- Formatting, Clippy with warnings denied, and the diff check passed.
- Tested stale edits, branch retention and resolution, new branches during review, delayed parents, partial/newer files, unknown fields, archive/restore, cycles, invalid paths, size limits, and competing local writers.
- Tested conflict copies with local files, not a live Dropbox account. No Linux or new native UI checks were performed.

### Next task

Add the versioned library manifest and status definitions. Then add the folder
reader and local index, retain the last valid state during sync, and connect
personal edits to the inspector. Keep Linux validation pending until it is run.

## Library manifest and status definitions

### Changes

- Added `LibraryStore` to create and inspect a new library folder with a versioned manifest and immutable definition history.
- Added the library name, default status, and ordered status definitions with stable keys and recommendation eligibility.
- Added guarded definition edits and explicit conflict resolution. Renaming a status does not rewrite game records.
- Reject status removal until game reassignment is available. Resolution must retain keys from every branch.
- Extracted the existing game revision mechanism for reuse by manifests. Game editing keeps its typed API; no dependency was added.
- Preserve unknown supported fields and reject partial, newer, or mixed-identity manifests without replacing them.
- Keep the native demo unchanged. Folder loading will connect definitions to game status validation and the interface.

### Validation

- macOS arm64: all 38 tests passed, including seven new library tests and the existing game/recovery tests after extraction.
- Formatting, Clippy with warnings denied, and the diff check passed.
- Tested create/reopen, relocation, existing-folder protection, label/order/default edits, invalid definitions, stale edits, conflicts, missing-current recovery, and unknown field preservation.
- Native Linux checks and live Dropbox synchronization remain pending.

### Next task

Build folder discovery and the local index. Validate discovered game status keys,
keep the last valid state during partial sync, and load the real library into the
existing grid and inspector. Add personal saves after that path is verified.

## Folder loading and native read-only libraries

### Changes

- Connected real library folders to the grid and inspector. Added a native folder picker, Open/Refresh shortcuts, a launch path option, and last-library reopening.
- Replaced demo-only presentation IDs with UUIDs. Use manifest status labels and order; keep search, selection, and viewport identity across metadata refreshes.
- Added one background reader with a device-local SQLite last-valid index. Source JSON and covers remain unchanged by browsing.
- Discover game files and renamed conflict copies once per scan. Validate revisions and status membership; preserve cached records when sources are missing, partial, conflicting, or unsupported. Game save checks share this discovery.
- Reused Eagle's picker, background task, watcher, cache clearing, and settings patterns. Use a bounded event channel, a short debounce, and a 10-second retry scan.
- Resolve relative covers within media/. Reject cover symlinks outside that folder and reload images when files change.
- Keep folder loading separate from the shell in a small UI module. Failed opens leave the previous reader active.
- Added a disposable disk-library generator for native QA. Added SQLite; the watcher uses the already locked notify version.
- A full rebuild exposed the missing standalone Apple Metal Toolchain. Enabled GPUI's built-in runtime shader path for macOS and verified the resulting native window.

### Validation

- macOS arm64: all 47 tests passed. Build, formatting, Clippy with warnings denied, launcher syntax, documentation links, and diff checks passed.
- Reader tests cover updates, source preservation, partial/missing files across restart, unavailable folders, undefined statuses, renamed conflicts, archive handling, index rebuild, relocation, and cover path boundaries.
- Native checks: loaded 12 games and covers from a disposable disk library; searched and selected Hades; observed changed status labels, rating, and description after external revision writes.
- Confirmed search and selection survived live refresh. Partial JSON retained valid details with a notice. Removing and restoring a cover produced the fallback and recovered image.
- Verified the native folder picker, invalid-folder feedback with the current collection retained, successful selection, and reopening the remembered library after quitting.
- Left the native app open on the disposable library. No personal Steam account or Obsidian vault was read or changed.
- Linux, live Dropbox sync, and real large-library scan timings remain unverified. The native accessibility limitation recorded above remains.

### Design review

| Before | After | Why |
| --- | --- | --- |
| Demo-only library label and fixed statuses | Library name and ordered manifest labels | Show the user's actual collection structure |
| No folder action | Native picker in the existing toolbar and menu | Reuse Eagle and platform conventions |
| Failed files could leave no current record to display | Last-valid details and a short notice | Keep browsing possible during partial sync |
| Status rows assume six entries | Scrollable status list | Keep custom statuses reachable in a short window |

### Next task

Connect status, rating, favorite, and text edits to the inspector. Keep drafts on
failure, confirm revision freshness, and show conflicts before enabling saves.
Library creation and conflict-resolution controls are still service operations;
the native interface remains read-only.

## Inspector drafts and guarded saves

### Changes

- Added an explicit Edit details form inside the Eagle inspector. Edit status, half-star rating, favorite, tags, notes, and description overrides.
- Keep one draft until Done or Discard. Refresh and selection changes do not replace it. Keep fields on save failure.
- Save on a background task through `LibraryStore::edit_game_personal`. Check the manifest, status membership, and expected game revision under local writer locks. Reuse immutable history and atomic publication.
- Preserve Steam data, covers, and unknown fields. Turning off the description override restores the retained provider text.
- Refresh the local index after a successful save. Block Save on stale or conflicting records. File issues currently block edits for the whole library; cover issues do not.
- Add Command-S / Control-S. Guard the Quit action, ordinary window close, and folder changes while a draft is dirty or saving.
- Drafts remain in memory. Forced exit and OS shutdown recovery are not implemented. Branch comparison and resolution controls remain deferred.

### Validation

- macOS arm64: all 50 tests passed, including three new integration tests through the inspector's write entry point. Final build, formatting, Clippy with warnings denied, documentation links, and diff checks passed.
- New tests cover all personal fields, reload, provider/unknown-field preservation, blank and removed overrides, undefined statuses, stale definitions, stale games, and a renamed remote conflict copy.
- Observed the native form, status/rating menus, and read-only details. Found compressed multiline fields; set explicit field heights and kept the action footer outside the scroll area.
- The user confirmed Save in the disposable library; its test note was present on disk. The user reported that Command-R appeared to do nothing. Moved refresh handling to app scope, added a toolbar button, and added visible progress/completion text.
- After restarting, native automation confirmed Command-R from both the grid and an input, refresh completion, corrected multiline layout, note entry, draft retention through refresh, Command-S save, and the Command-Q dirty-draft guard.
- Replaced a brief false conflict after a successful save with a waiting-for-refresh message. The reader can lag behind a confirmed disk write.
- Native automation was unreliable while testing earlier processes. No new UI claim relies only on a click attempt. The final build also passed restart persistence, Discard, and the ordinary close-button draft guard. Short-window scrolling remains unverified.
- Linux and live Dropbox checks remain pending. No personal Steam account or Obsidian vault was changed.

### Design review

| Before | After | Why |
| --- | --- | --- |
| Read-only inspector | Explicit Edit details, Save, and Discard | Make writes deliberate and easy to cancel |
| Provider description only | Separate personal override toggle | Keep Steam text available |
| Short multiline fields | Fixed field heights and a separate action footer | Keep text and save actions reachable |
| Refresh could replace an edit form | One retained draft with stale-record feedback | Protect work during sync |

### Next task

Add focused conflict comparison and explicit branch selection inside the existing
inspector. Complete the remaining native interaction checks with that pass. Keep Linux verification ahead of
new screens.

## Five-star rating control

- Replaced the inspector rating dropdown with five clickable stars. Click a star to choose a whole-star rating. Click the selected star again to clear it.
- Keep existing half-star records intact and display their partial fill. No data migration or new dependency.
- Reused GPUI buttons for focus, keyboard activation, disabled state, and hover feedback.
- macOS: build, formatting, Clippy with warnings denied, and diff checks passed. Native checks confirmed five stars, selection, clear, and Discard. Test changes were discarded.

| Before | After | Why |
| --- | --- | --- |
| Dropdown with eleven numeric choices | Five direct star buttons | Set a rating with one click |

## Commit checkpoint

- Commit the versioned records, library definitions, folder loading, local index, inspector saves, refresh fix, and five-star control together.
- Include the existing app icon asset. The launcher does not use it yet.
- Validation already completed: 50 tests, native macOS interaction checks, build, and Clippy with warnings denied. Recheck formatting and the diff before commit.
- Remaining work: conflict comparison/resolution UI, Linux runtime verification, live Dropbox checks, and short-window editor checks.
